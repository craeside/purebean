<svg class="icon icon-{{ icon }}" aria-label="{{ icon | replace: '-', ' ' }} icon">
  <use xlink:href="#icon-{{ icon }}">
    {%- if title != blank -%}
      <title>{{ title }}</title>
    {%- endif -%}
  </use>
</svg>